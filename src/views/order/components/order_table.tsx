
import { ReactElement, ReactNode } from 'react';
import './index.scss'

const OrderTable = () : ReactElement<ReactNode> => {
    return (
        <div className='order-table'>
            订单列表
        </div>
    )
};

export default OrderTable;