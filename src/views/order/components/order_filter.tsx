
import { ReactElement, ReactNode } from 'react';
import './index.scss'

const OrderFilter = () : ReactElement<ReactNode> => {
    return (
        <div className='order-tilter'>
            订单筛选
        </div>
    )
};

export default OrderFilter;